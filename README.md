# HTTP Web Server (in development)

## Small web server which currently have the following features:

### - Parsing the GET requests (currently only GET requests are allowed)
### - Serving the HTML pages
### - ...

